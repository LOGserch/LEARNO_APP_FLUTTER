import 'package:flutter/material.dart';

class ConfMatScreen extends StatelessWidget {
  const ConfMatScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ConfMatScreen'),
      ),
    );
  }
}
